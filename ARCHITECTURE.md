# 多智能体交易分析框架 — 架构设计文档

## 一、设计理念

### 核心洞察

单一 LLM 做投资分析时，倾向于给出"平衡"但缺乏深度的结论 — 既说利好又说风险，最终建议"谨慎观望"。这是因为一个模型在单次推理中同时扮演多个角色时，会自然地趋向中庸。

**解法：** 将投资决策分解为多个专业角色，每个角色有明确立场和职责，通过结构化的对抗辩论产生高质量决策。这模拟了真实交易公司的组织架构 — 分析师提供情报，研究员辩论方向，交易员制定方案，风控把关，投组经理拍板。

### 设计原则

1. **角色专注** — 每个 agent 只做一件事，有明确的立场约束（多头必须看多，空头必须看空）
2. **对抗产出质量** — 辩论迫使每方找到最强论据并回应对方最强攻击，消除确认偏误
3. **数据与推理分离** — 数据获取是确定性操作（Python 脚本），推理是 LLM 的工作，不混为一谈
4. **分层决策** — 信息收集 → 方向辩论 → 执行方案 → 风险评估 → 最终裁决，层层递进
5. **成本意识** — 只在"裁判"角色使用强模型（opus），其余用快速模型（sonnet）

---

## 二、流水线架构

```
┌─────────────────────────────────────────────────────────────┐
│                    Phase 0: 数据预取                         │
│         Python (yfinance + stockstats) → 结构化数据包        │
└────────────────────────┬────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              Phase 1: 分析师团队 (4 Agent 并行)              │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────────┐   │
│  │ 市场/技术 │ │ 舆情/情绪 │ │ 新闻/宏观 │ │ 基本面分析师 │   │
│  └────┬─────┘ └────┬─────┘ └────┬─────┘ └──────┬───────┘   │
│       └────────────┴────────────┴───────────────┘           │
│                         ▼ 汇总报告                           │
└─────────────────────────────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              Phase 2: 多空研究辩论 (顺序)                    │
│                                                             │
│  多头研究员 ──→ 空头研究员 ──→ (重复 N 轮)                    │
│       │              │                                      │
│       └──────────────┘                                      │
│                ▼                                            │
│       研究经理裁决 (opus) → 投资计划                          │
└─────────────────────────────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              Phase 3: 交易员提案 (顺序)                      │
│                                                             │
│  交易员 → 具体执行方案 (入场/止损/止盈/仓位/风险回报比)       │
└─────────────────────────────────────────────────────────────┘
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              Phase 4: 风控辩论 + 最终决策                     │
│  ┌────────┐  ┌────────┐  ┌────────┐                        │
│  │ 激进派  │  │ 保守派  │  │ 中立派  │  ← 3 Agent 并行       │
│  └───┬────┘  └───┬────┘  └───┬────┘                        │
│      └───────────┴───────────┘                              │
│                   ▼                                         │
│       投组经理最终决策 (opus)                                 │
│       → Buy / Overweight / Hold / Underweight / Sell        │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、12 个角色设计

### 3.1 分析师团队（信息收集层）

分析师的职责是**客观呈现数据和事实**，不做方向性判断。每个分析师只看自己领域的数据切片。

| 角色 | 输入数据 | 输出 | 关键指令 |
|------|---------|------|---------|
| 市场/技术分析师 | OHLCV + 技术指标 | 趋势、均线、RSI、MACD、布林带、支撑阻力、展望 | "引用具体指标值" |
| 舆情/情绪分析师 | 新闻列表 + 内幕交易 | 情绪评分(1-10)、叙事分析、风险信号 | "对每条新闻评估股价影响" |
| 新闻/宏观分析师 | 新闻 + 公司背景 | 宏观环境、行业动态、催化剂、事件日历 | "区分短期和中期影响" |
| 基本面分析师 | 财务报表 + 公司概览 | 估值、盈利质量、资产负债、现金流、分析师共识 | "引用具体财务数据" |

**设计要点：**
- 4 个分析师完全独立，可并行执行
- 每个分析师末尾必须附 Markdown 表格总结，方便下游角色快速提取
- 分析师不做买卖建议，只呈现事实和信号

### 3.2 研究辩论团队（方向判断层）

研究员的职责是**带立场地论证**，通过对抗产出高质量的方向判断。

| 角色 | 立场 | 核心指令 | 模型 |
|------|------|---------|------|
| 多头研究员 | 必须看多 | "为投资构建强论证，反驳空头担忧" | sonnet |
| 空头研究员 | 必须看空 | "逐一反驳多头论点，揭示过度乐观" | sonnet |
| 研究经理 | 裁判 | "评估辩论，做出明确决策，制定投资计划" | **opus** |

**辩论机制：**
- 多头先发言 → 空头必须**逐一回应**多头论点（不能各说各话）
- 可配置多轮辩论（默认 1 轮 = 多头 + 空头各 1 次）
- 研究经理必须做出明确决策（Buy/Sell/Hold），不允许无故"持有"
- 研究经理输出**具体投资计划**（价位、仓位、止损止盈），供交易员执行

**为什么多头先发：** 多头需要主动构建论证，空头基于多头论点进行质疑更自然。先空后多也可以，但"攻击已有论点"比"预判对手论点"更容易产出高质量反驳。

### 3.3 交易员（执行方案层）

| 角色 | 输入 | 输出 | 核心指令 |
|------|------|------|---------|
| 交易员 | 分析师报告 + 投资计划 + 持仓数据 | 具体交易提案 | "末尾必须写 最终交易提案: BUY/HOLD/SELL" |

**设计要点：**
- 交易员将研究经理的方向性判断转化为可执行的交易方案
- 必须包含：入场价、仓位比例、止损、分阶段止盈、风险回报比、触发条件
- 整合实盘持仓数据（如有）

### 3.4 风控辩论团队（风险评估层）

风控团队的职责是**从风险角度审视交易员的方案**，三个立场形成张力场。

| 角色 | 立场 | 核心指令 | 字数限制 |
|------|------|---------|---------|
| 激进派 | 嫌仓位太小 | "论证应更激进，质疑保守策略错失机会" | 500字 |
| 保守派 | 嫌风险太大 | "论证应更谨慎，质疑止损过远、基本面被低估" | 500字 |
| 中立派 | 寻找平衡 | "提出中间方案，做三种情景分析" | 500字 |

**设计要点：**
- 3 个风控辩论可以**并行**执行（与多空辩论不同，风控三方不需要逐一回应）
- 字数限制 500 字，避免冗长 — 风控辩论的价值在于尖锐观点，不在于长篇论述
- 中立派必须做量化情景分析（概率 × 收益），给投组经理提供决策依据

### 3.5 投资组合经理（最终决策层）

| 角色 | 输入 | 输出 | 模型 |
|------|------|------|------|
| 投组经理 | 全部报告 + 交易提案 + 风控辩论 | 5级评级 + 执行摘要 + 投资论题 | **opus** |

**5 级评级体系：**

| 评级 | 含义 | 行动 |
|------|------|------|
| Buy | 强烈看好 | 建仓或加仓 |
| Overweight | 看好 | 逐步增加敞口 |
| Hold | 中性 | 维持现有仓位 |
| Underweight | 看淡 | 逐步减少敞口 |
| Sell | 强烈看空 | 清仓或避免入场 |

**输出格式要求：**
1. 评级（5 选 1）
2. 执行摘要（表格：仓位、止损、目标、时间框架、强制退出条件）
3. 投资论题（裁决理由，必须引用各方辩论的具体论点）

---

## 四、数据层设计

### 预取 vs 工具调用

原版 TradingAgents 让每个分析师 LLM 自行决定调用哪些数据工具（tool-calling loop），每个分析师需要 2-3 次 LLM 往返。

**本框架选择预取：** 所有数据在 Phase 0 一次性获取，按分析师需求格式化后直接注入 prompt。

| 对比 | Tool-calling | 预取 |
|------|-------------|------|
| LLM 调用数 | ~21 | **12** |
| 数据完整性 | LLM 可能漏调某些工具 | 全量获取，不遗漏 |
| 成本 | 高（每次 tool call 都是 LLM 往返） | 低 |
| 灵活性 | LLM 可动态决定 | 固定数据集 |
| 可靠性 | 依赖 LLM 正确调用 | 确定性脚本 |

预取的代价是灵活性略低（分析师不能"追问"数据），但对于标准化的股票分析场景，数据需求是可预测的，预取更高效。

### 数据源

```python
yfinance:
  - OHLCV (history)          → 市场分析师
  - info (公司概览)            → 基本面分析师
  - quarterly_balance_sheet   → 基本面分析师
  - quarterly_cashflow        → 基本面分析师
  - quarterly_income_stmt     → 基本面分析师
  - news                      → 情绪分析师 + 新闻分析师
  - insider_transactions      → 情绪分析师

stockstats:
  - RSI, MACD, SMA, EMA       → 市场分析师
  - 布林带, ATR                → 市场分析师
```

### 格式化策略

每个分析师收到的数据是**预格式化的 Markdown**，而非原始 JSON/CSV。这样 LLM 可以直接"阅读"数据，不需要花 token 解析格式。

```
# LI 市场数据 (2026-03-26)

## OHLCV 价格数据（近期）
```csv
Date,Open,High,Low,Close,Volume
...
```

## 技术指标（最新值）
- rsi_14: 54.40
- macd: -0.061
...
```

---

## 五、并行策略

### 可并行的环节

| 环节 | 并行方式 | 原因 |
|------|---------|------|
| 4 个分析师 | **并行** | 各自独立，数据切片不同 |
| 多空辩论 | **顺序** | 空头必须看到多头论点后反驳 |
| 3 个风控辩论 | **并行** | 三方各自独立发表观点 |
| 研究经理/投组经理 | **顺序** | 必须等前序环节完成 |

### 并行带来的时间优化

```
顺序执行: 12 × ~1.5min = ~18min
并行优化: Phase1(1.5min) + Phase2(4.5min) + Phase3(1.5min) + Phase4(3min) = ~10.5min
实测: ~6min (Agent Tool) / ~15min (claude -p subprocess)
```

---

## 六、模型分层策略

| 角色类型 | 模型 | 原因 |
|---------|------|------|
| 分析师 (4个) | sonnet | 数据整理和信号识别，不需要深度推理 |
| 多头/空头研究员 | sonnet | 论证构建，sonnet 足够 |
| 交易员 | sonnet | 方案制定，结构化输出 |
| 风控辩论 (3个) | sonnet | 短观点输出（500字），不需要 opus |
| **研究经理** | **opus** | 裁判角色，需要权衡复杂对立论点 |
| **投组经理** | **opus** | 最终决策，需要综合所有信息做判断 |

**成本估算（每次分析）：** 10 次 sonnet + 2 次 opus

---

## 七、记忆与学习系统

### BM25 记忆

每个角色有独立的记忆文件，存储过去的分析情景和反思结论：

```
memory/
  bull.json       # 多头历史反思
  bear.json       # 空头历史反思
  trader.json     # 交易员历史反思
  judge.json      # 研究经理历史反思
  portfolio.json  # 投组经理历史反思
```

**检索方式：** BM25 词法匹配（rank-bm25 库），无需 embedding API。输入当前情景文本，返回最相似的 2 条历史记录。

**为什么选 BM25 而非 embedding：**
- 金融文本共享大量专业词汇（ticker、指标名、市场状态），BM25 的词频匹配天然适合
- 无需额外 API 调用，纯离线计算
- 记忆规模小（数百条），BM25 足够

### 反思循环

```
交易执行 → 观察结果 → reflect.py → 各角色反思 → 写入记忆
                                                    ↓
下次分析同标的 → BM25 检索 → 注入 prompt → 避免重复犯错
```

反思 prompt 模板：
```
你是复盘分析师。评估 "{角色名}" 在以下交易中的表现。
实际结果: {outcome}
分析: 哪些判断正确？哪些错误？下次如何改进？
```

---

## 八、Prompt 设计原则

### 角色约束

每个角色的 prompt 必须包含**明确的立场约束**，否则 LLM 会自动"平衡"。

```
❌ "分析这只股票的优劣势"     → 产出中庸报告
✅ "你是多头，必须论证为什么应该买入" → 产出尖锐论证
```

### 对抗指令

辩论角色的 prompt 必须包含**反驳指令**：

```
✅ "如有空头已提出论点，必须正面回应并反驳"
✅ "逐一反驳多头论点，用具体数据揭示其过度乐观之处"
```

没有反驳指令，辩论会退化为两段独立分析。

### 结构化输出

关键角色要求**表格化输出**，确保信息可被下游角色快速提取：

```
✅ 分析师 — 末尾附 Markdown 表格总结
✅ 投组经理 — 执行摘要必须是表格
✅ 交易员 — "最终交易提案: BUY/HOLD/SELL"
```

### 字数控制

风控辩论限制 500 字。太长会稀释观点锐度，太短无法充分论证。500 字是一个平衡点。

---

## 九、两种运行模式

### 模式 A: Claude Code Agent Tool（交互式）

```
用户输入: /deep-analysis TSLA
                ↓
Claude Code 读取 ~/.claude/commands/deep-analysis.md
                ↓
按 prompt 指令依次执行 Phase 0-4
  - Phase 1: 4 个 Agent 并行（一条消息 4 个 tool call）
  - Phase 4: 3 个 Agent 并行
  - 研究经理/投组经理: model="opus"
                ↓
最终决策输出到对话
```

**优势：** 更快（~6min）、Agent 可访问文件系统和工具、可交互追问
**限制：** 只能在 Claude Code 交互会话中使用

### 模式 B: Python 编排器 + claude -p（脚本式）

```
run_analysis.sh LI
        ↓
orchestrator.py
  - Phase 1: ThreadPoolExecutor 并行 4 个 claude -p
  - Phase 2-4: 顺序 claude -p 调用
        ↓
results/LI/date_decision.md
```

**优势：** 可被 cron/OpenClaw 调度、结果自动存档为 JSON
**限制：** 更慢（~15min）、风控辩论只能顺序执行

### 如何选择

| 场景 | 推荐模式 |
|------|---------|
| 临时想分析某只股票 | A (交互式) |
| 定时批量分析关注列表 | B (脚本式) |
| 分析后想追问细节 | A (交互式) |
| 需要结果存档/回溯 | B (脚本式) |

---

## 十、可扩展方向

1. **多标的组合分析** — 对 watchlist 中的多只股票并行运行，最后增加一个"组合优化 Agent"做跨标的配置
2. **实时数据接入** — 接入 Futu OpenD 的实时行情，替换 yfinance 的延迟数据
3. **自动复盘闭环** — 接入交易记录，自动触发 reflect.py，形成真正的学习循环
4. **行业专家 Agent** — 针对特定行业（如新能源车、半导体）增加行业专家角色
5. **期权策略 Agent** — 在交易员之后增加期权策略设计环节
